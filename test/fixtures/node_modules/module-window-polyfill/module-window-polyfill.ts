/* eslint-env browser*/
// @ts-nocheck
if (typeof window.EXAMPLE_MODULE_POLYFILL !== "undefined") {
	window.EXAMPLE_MODULE_POLYFILL = "ok multiple";
} else {
	window.EXAMPLE_MODULE_POLYFILL = "ok once";
}
