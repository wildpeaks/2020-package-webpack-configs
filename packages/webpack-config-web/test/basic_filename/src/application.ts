/* eslint-env browser */
const container = document.createElement("div");
container.setAttribute("id", "mocha");
container.innerText = "Basic Filename";
document.body.appendChild(container);

export {};
